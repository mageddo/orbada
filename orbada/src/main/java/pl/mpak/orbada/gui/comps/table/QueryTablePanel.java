/*
 * QueryTablePanel.java
 *
 * Created on 21 styczeñ 2008, 20:32
 */

package orbada.gui.comps.table;

import java.io.Closeable;
import java.io.IOException;
import pl.mpak.usedb.core.Database;
import pl.mpak.util.Titleable;

/**
 *
 * @author  akaluza
 */
public class QueryTablePanel extends javax.swing.JPanel implements Titleable, Closeable {
  
  private String sqlText;
  private Database database;
  private String title;
  
  /** Creates new form QueryTablePanel */
  public QueryTablePanel(String sqlText, Database database, String title) throws Exception {
    this.sqlText = sqlText;
    this.database = database;
    this.title = title;
    initComponents();
    init();
  }

  public QueryTablePanel(String sqlText, Database database) throws Exception {
    this(sqlText, database, null);
  }

  private void init() throws Exception {
    queryTable.getQuery().setDatabase(database);
    queryTable.getQuery().open(sqlText);
  }
  
  public void close() throws IOException {
    queryTable.getQuery().close();
  }

  public void setTitle(String title) {
    this.title = title;
  }
  
  public String getTitle() {
    return title;
  }
  
  /** This method is called from within the constructor to
   * initialize the form.
   * WARNING: Do NOT modify this code. The content of this method is
   * always regenerated by the Form Editor.
   */
  // <editor-fold defaultstate="collapsed" desc="Generated Code">//GEN-BEGIN:initComponents
  private void initComponents() {

    jScrollPane1 = new javax.swing.JScrollPane();
    queryTable = new DataTable();
    statusBar = new pl.mpak.usedb.gui.swing.QueryTableStatusBar();

    setLayout(new java.awt.BorderLayout());

    jScrollPane1.setViewportView(queryTable);

    add(jScrollPane1, java.awt.BorderLayout.CENTER);

    statusBar.setTable(queryTable);
    add(statusBar, java.awt.BorderLayout.SOUTH);
  }// </editor-fold>//GEN-END:initComponents
  
  
  // Variables declaration - do not modify//GEN-BEGIN:variables
  private javax.swing.JScrollPane jScrollPane1;
  private DataTable queryTable;
  private pl.mpak.usedb.gui.swing.QueryTableStatusBar statusBar;
  // End of variables declaration//GEN-END:variables

}
